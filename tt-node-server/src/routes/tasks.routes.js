import { Router } from 'express';
// TODO: Importa los controladores que implementarás


const router = Router();

/**
 * TODO: Define las siguientes rutas:
 *
 * Tareas Obligatorias:
 * - GET /api/tasks/stats - Obtener estadísticas (agregación)
 * - GET /api/tasks - Listar todas las tareas
 * - POST /api/tasks - Crear nueva tarea
 *
 * Tareas Bonus:
 * - PUT /api/tasks/:id - Actualizar tarea
 * - DELETE /api/tasks/:id - Eliminar tarea
 *
 */

// Implementa aquí tus rutas

export default router;
