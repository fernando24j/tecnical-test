// TODO: Importa el modelo Task desde Task.model.js
// import { Task } from '../models/Task.model.js';

/**
 * TODO: Implementar los siguientes controladores
 * 
 * 1. getTasks - GET /api/tasks
 *    - Listar todas las tareas
 *    - Ordenar por fecha de creación (más recientes primero)
 *    - BONUS: Soportar filtrado por category y status
 *    - BONUS: Soportar paginación (page, limit)
 * 
 * 2. createTask - POST /api/tasks
 *    - Crear nueva tarea
 *    - Validar campos requeridos (title, category)
 *    - Retornar la tarea creada
 * 
 * 3. getTaskStats - GET /api/tasks/stats
 *    - Usar MongoDB Aggregation Framework
 *    - Retornar:
 *      - Total de tareas por categoría
 *      - Total de tareas por status (pending/completed)
 *      - Últimas 5 tareas creadas
 * 
 * 4. updateTask - PUT /api/tasks/:id (BONUS)
 *    - Actualizar una tarea existente
 *    - Validar que la tarea existe
 * 
 * 5. deleteTask - DELETE /api/tasks/:id (BONUS)
 *    - Eliminar una tarea
 *    - Validar que la tarea existe
 */

// Implementa aquí tus funciones
